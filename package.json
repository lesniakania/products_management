{
  "name": "@products-management/root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start-api": "pnpm --filter api start",
    "build-api": "pnpm --filter api build",
    "start-frontend": "pnpm --filter frontend start"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
